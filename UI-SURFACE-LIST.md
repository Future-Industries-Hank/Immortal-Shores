# UI-SURFACE-LIST — FULL VISUAL OVERHAUL

**Date:** 2026-08-03 · from 5-agent audit · maintain checkboxes as surfaces are skinned · **R2 column** = round-2 polish pass (judge findings: UI 5.4, map 3.6, mobile 3.0)

| Done | Surface | Severity | Fix (audit) | R2 |
|---|---|---|---|---|
| [x] | Global typography & design tokens | major | Self-host two faces (e.g. a chiselled serif like 'Cormorant'/'Marcellus' for H1-H3 and numerals-tabular sans for data), add tokens --radius-sm/md/lg,  | — |
| [x] | Login / Found-settlement panel | major | Full-bleed painted/gradient river backdrop (canvas already exists — render the idle scene behind auth), logotype in the display serif with a gold rule | [x] palms redrawn: filled silhouettes, curved trunk + 8 tapered fronds |
| [x] | HUD resource strip | major | Per-resource inline SVG glyph (16px, single gold-line style) + tabular-nums value; tooltip already has full label — add a +x.x/h trend from s.producti | [x] mobile: 4-col compact chip grid (2 rows), labels hidden <480px |
| [x] | Seals / province chip | major | Gold or papyrus text with a small carved-seal SVG (crimson wax circle with impression), province name in muted papyrus; merge the duplicate rule; make | — |
| [x] | Quality / Time-of-day selects | minor | Move both into a Settings popup (gear icon button); replace tod with a compact icon cycle-button (sun/dusk/moon glyphs) if it stays; give postcard an  | [x] appearance:none + gold chevron + chip chrome; wrap (no more clipping) on narrow screens |
| [x] | Bottom nav (Shore/Harbor/Tablets/Allies/Wall/Build/Military) | major | Icon-above-label buttons (etched gold-line SVGs: shore hut, barge, tablet, handshake, wall, trowel, spear), active = gold underline notch + brightened | [x] mobile: horizontal scroll + right fade & chevron affordance — Build/Military reachable |
| [x] | Menu popup shell | minor | Header gets the display serif, a small panel glyph, and a hairline double-rule; add subtle papyrus texture to the body; consider docking left as a col | [x] width capped to 100vw−margins; bottom padding so text no longer clips mid-sentence |
| [x] | Shore (settlement) panel content | critical | Building rows become cards: icon, name in serif, level pip row, worker stepper as one segmented control, single overflow menu for upgrade; move Dark/C | [x] auto "username's Shore" → "Your Shore"; secondary buttons get crafted gradient/inset/pressed chrome |
| [x] | Tablet Wall — chat | critical | Grow box to fill panel height; message = avatar-seal initial, name in channel-tinted small-caps, timestamp; own messages tinted papyrus-deep; mention  | [x] placeholder "Write to your province…"; papyrus empty state; channel pills; search field w/ icon |
| [x] | Tablet Wall — trust offers & market forms | critical | Offer cards: give-side icons → arrow glyph → want-side icons, poster name + reputation stars, Accept as gold primary; composer becomes a two-column gi | [x] market select gets shared crafted select chrome |
| [x] | Tablets panel (gifts + mail inbox) | critical | Style mail as sealed-tablet cards (papyrus texture, wax-seal Accept button that 'breaks' on claim, sender + relative time); gift composer gets a partn | — |
| [x] | Allies panel | critical | Split into sub-tabs (Partners / Circles / Events / Legacy); partner rows get seal-avatar, trade count as gold pips, Prefer becomes a toggleable star;  | [x] notifications = iconed cards (type glyph, rel. timestamp), latest 5 + "earlier…" collapse |
| [x] | Military & Monuments panel | major | Unit cards with glyph (bow/spear/chariot), count, upkeep/h and train button showing cost inline; monuments as banner cards using site display names; s | [x] "Dispatch envoy (GH7+)" → "— needs Great House Level 7" |
| [x] | World Map panel + site list | major | Make the SVG the interface: plot sites as clickable markers on the river with hover cards, pan on wide layout; list becomes a filterable legend; use t | [x] sites now plotted as river markers; card ↔ marker hover/selection sync both directions |
| [x] | Inspect popup (building / construction / pad / generic) | major | Header band with building glyph + serif title + level pips; convert sections to tabs (Overview / Levels / Actions) so first paint fits without scroll; | — |
| [x] | Tutorial panel | major | Anchor steps to targets (dim viewport, spotlight the Shop pad / nav tab in question), progress as 5 seal-dots, advisor voice with small scribe glyph;  | — |
| [x] | First Week Goals panel | major | Collapse to a slim quest tracker: header 'First Week' with 4/6 progress ring, rows with gold check-seal animation on completion, collapsible chevron i | — |
| [x] | Toast / hint / ration warning layer | major | Toast stack top-right with icon + tint per type, 250ms slide-fade, queue max 3; ration-critical gets a warning glyph and slow pulse animation plus a c | — |
| [x] | Dark mode & color-blind themes | critical | Route every surface background through tokens (--surface-card, --surface-field) and audit each component under html.dark; move all inline colors (form | [x] all R2 components tokenized w/ html.dark overrides + focus-visible states |
| [x] | Mobile / narrow layout | major | Add `padding-bottom: env(safe-area-inset-bottom)` to #hub and inset-top to #hud-top; replace hidden scroll with wrap-into-More (nav) and grouped popov | [x] HUD chip grid, selects wrap, nav scroll+fade, panel width caps + min-width:0 wraps |
| [x] | Production overlay | major | Retitle 'Production', use BUILDING_TITLE for names, small-caps styled header row, color the drain column red-tint and paused rows amber; move toggle i | — |
| [x] | Harbor panel & barge rows | major | Name barges procedurally ('Reed Dancer'), style the cargo slider (gold track, amount readout), ETA as 'arrives in 26m', destination picker from map si | [x] "+ 20 wh" jargon → "· 20 worker-hours" |
| [x] | Build panel (instruction wall) | major | (R2 finding) prose instruction wall → visual palette | [x] card grid from NEW_BUILD_COST: glyph roundel, cost line w/ icons, pad-type chip, Built state, "Click a sand pad…" lead |
| [x] | River board (the map itself) | critical | Replace with an authored board: (a) a painted background raster (papyrus/parchment texture, 2x, embedded or asset-loaded) or a richly authored SVG wit | [x] v2 board 640×620 (~2× taller): layered desert, dune ridges, delta fan + sea, cataract rapids, palm/reed clusters, dashed province borders, gold wash on your reach |
| [x] | Province markers on the SVG | critical | Place provinces at authored coordinates ON the river (or derive t-values along the path via getPointAtLength) so each sits on a bank. Replace bare cir | [x] arc-length-placed on banks, bigger ziggurat roundels, small-caps serif plaque labels, wax seal on yours |
| [x] | Site list ('map-grid') and marker icon set | major | Ship a small inline-SVG icon set (16-20px, single stroke style, 4 glyphs: city ziggurat, monument obelisk, founding stake/flag, generic cartouche) ren | [x] per-site markers on the board (kind-tinted, dashed ring = open shore), filters mirror onto markers |
| [x] | Legend ('Pads' line) | major | Delete this line or replace it with a real legend for the map's own marker kinds, using the exact same SVG roundel icons as the site list/board marker | — |
| [x] | Hover / selection / feedback model | major | Add a .selected class on the active site card (gold outline + slight lift) kept while its popup is open, cleared in onClose; mirror it on the correspo | [x] hover halo + selected ring on markers, mirrored to cards (.hl/.selected), keyboard operable |
| [x] | Panel copy / framing | minor | Rewrite in-world: 'Six provinces line the Eternal River; newcomers are settled where their luxury is scarcest, so every shore has something to trade.' | [x] dev-tell sweep: chat placeholder, "wh", "GH7+", raw username title |

**2026-08-03 skin pass caveats:** toast remains a single element (text set by main.ts — typed queue needs main.ts, other workstream); tutorial has seal-dot progress + scribe voice but no viewport spotlight anchoring; harbor launch destination still own-shore only (API-behavior change out of scope); ~~map sites are filterable legend cards linked to province markers, not yet plotted as individual board markers~~ **R2: sites are now plotted as individual board markers with two-way card↔marker highlight.**

**R2 pass notes (2026-08-03):** barge button reads "20 worker-hours" (accurate expansion of "20 wh" — it is the build labor, not a warehouse-level term). Chat channel pills drive a visually-hidden `#chat-channel` select so all existing send handlers keep working. Board stays papyrus-light in dark mode by design (it reads as an in-world artifact); all chrome around it is theme-aware.
